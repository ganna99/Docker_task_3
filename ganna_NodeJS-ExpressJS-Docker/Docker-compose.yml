version: '3.8'

services:
  express_api:
    build: .
    container_name: express_api
    ports:
      - "3000:3000"
    environment:
      - MONGO_URI=mongodb://mongo:27017/ganna
    volumes:
      - .:/app
      - /app/node_modules
    command: npm run dev   
    depends_on:
      - mongo

  mongo:
    image: mongo:latest
    container_name: mongo
    ports:
      - "27017:27017"
    volumes:
      - mongo-data:/data/db

volumes:
  mongo-data: